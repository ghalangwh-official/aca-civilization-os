# Edge Config

Put hardware-specific limits and static safety parameters here.

- `robot_profile.example.yaml` is the baseline template.
- Copy it to `robot_profile.yaml` for the actual robot.
- Keep operational secrets and per-device overrides out of git.
